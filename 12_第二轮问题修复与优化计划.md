# 个人用药助手：第二轮问题修复与优化计划

日期：2026-09-19  
性质：依据用户真机反馈制定的修复执行计划  
目标：先修复阻断使用的问题，再替换失败的 OCR 路线，最后完成一轮真机导向的 UI 与稳定性优化。

## 1. 本轮必须纠正的产品方向

1. 应用暂定名称统一为“个人用药助手”。“Luna Max”只是开发时使用的模型/代理名称，绝不能作为产品名、通知标题、备份文件名或 AI 身份。
2. 删除本地 ML Kit OCR 和基于正则表达式猜测药品字段的路线。
3. 拍照或导入药盒、药瓶、说明书和 PDF 后，统一由 `deepseek-flash` 的视觉能力进行结构化识别。
4. 原始图片、PDF、AI 原始结果、结构化结果和用户修订结果全部保存在 App 私有目录与本地数据库中。
5. AI 识别结果始终是草稿；用户确认后才能写入正式药物、药盒批次和说明书资料。
6. API Key 本地保存与调用必须真正可用，并将“保存密钥”和“测试连接”拆成两个独立动作。

---

## 2. 已确认的问题与技术原因

### 2.1 API Key 保存失败

当前 `DataCipher` 使用 Android Keystore 的 AES KeyGenerator，却调用普通的 `generator.init(256)`。Android Keystore 的 AES 密钥通常必须通过 `KeyGenParameterSpec` 指定用途、GCM 模式、NoPadding 和密钥长度。现有实现可能在真机上直接抛出 `Secure storage is unavailable`。

修复要求：

- 使用 `KeyGenParameterSpec.Builder` 正确创建 AES/GCM 密钥。
- 用途必须包含 encrypt/decrypt，BlockMode 为 GCM，Padding 为 NoPadding。
- 保存后立即执行“加密—读取—解密—常量时间或安全比较”的本地自检。
- 自检失败时不显示模糊的“保存失败”，而要显示可操作原因，并保留错误码供调试；不得显示 Key 原文。
- 保存密钥不依赖网络。即使断网，仍应能够显示“已安全保存，尚未测试连接”。
- 单独提供“测试 DeepSeek 连接”按钮，用最小请求检查认证与模型可用性。
- 将 401、402/余额、403、429、超时、DNS、TLS 和服务端错误分别转成用户可理解的中文提示。
- 支持查看状态、替换和删除 Key；绝不提供明文回显。
- 旧版本中无法解密或格式损坏的密钥应被安全清除，并提示用户重新输入，不能导致整个设置页崩溃。

验收标准：

- 真机上输入 Key 后能保存；退出并重新打开 App 后仍显示“已保存”。
- 断网保存成功，连接测试明确提示网络不可用。
- 正确 Key 可通过一次最小在线测试；错误 Key 明确提示认证失败。
- 删除后 `hasKey()` 为 false，历史日志、备份和 APK 不包含 Key。

### 2.2 页面与系统状态栏重叠

应用 targetSdk 35，在 Android 15 的 edge-to-edge 行为下不能依赖固定的 10dp 顶部 padding。当前根视图未消费系统栏 Insets，因此标题会与时间、Wi-Fi、电量等状态栏内容重叠。

修复要求：

- 使用 `WindowInsets`/`WindowInsetsCompat` 获取状态栏、导航栏和显示挖孔安全区域。
- 将顶部系统栏 inset 应用到根容器或 App Bar；将底部导航栏 inset 应用到底部导航。
- 适配刘海、打孔屏、手势导航、三键导航、横屏、浅色和深色状态栏图标。
- 不使用某一台手机专用的固定 padding。
- 输入法弹出时表单内容可滚动，保存按钮不得被键盘遮挡。

验收标准：

- 标题、返回按钮和顶部操作不与状态栏重叠。
- 底部导航不与系统手势条或三键导航重叠。
- 浅色/深色和至少两种屏幕尺寸布局正常。

### 2.3 产品名称错误

将所有用户可见的 “Luna Max” 替换为“个人用药助手”，包括：

- App label、首页标题、关于页面和版本信息。
- 通知渠道、通知标题和公开锁屏通知。
- 导出/导入文件名、错误提示和说明文档。
- 内置医疗安全 system prompt 中的身份描述。
- AI 应自称“个人用药助手中的 AI 功能”，模型只在设置的技术信息中显示为 `deepseek-flash`。

内部 Java 包名和数据库文件名如果修改会带来迁移风险，可以暂时保留，但不得继续暴露到 UI 或导出文件名。

验收标准：对源码、资源、APK 解包文本执行用户可见字符串扫描，不得再出现把 “Luna Max” 当产品名的内容。

---

## 3. 用 DeepSeek Flash 视觉识别替换本地 OCR

### 3.1 删除旧路线

- 移除 ML Kit 中文 OCR 和条码扫描依赖。
- 删除或停用 `OcrService` 及其正则字段猜测逻辑。
- UI 不再出现“本地 OCR”“OCR 置信度”等误导性表述，统一称为“AI 图片识别草稿”。
- 旧 OCR 资料继续可查看，但标记为“旧版识别结果”，允许用户重新提交给 DeepSeek 识别。

### 3.2 新的识别流程

1. 用户拍照或选择图片/PDF。
2. App 将原始文件复制到应用私有目录，计算 SHA-256，保存来源、时间、页码和 MIME 类型。
3. 图片自动校正方向，并在不影响小字可读性的前提下压缩；显示将要上传的页数和估算大小。
4. 用户确认“本次将把这些图片发送给 DeepSeek 识别”。
5. App 使用已保存的 DeepSeek Key 调用唯一模型 `deepseek-flash` 的视觉输入能力。
6. PDF 在本地逐页渲染为图片，按可控批次提交，避免请求过大；失败页可单独重试。
7. 要求模型使用严格 JSON Schema 返回结构化结果。
8. App 验证 JSON 类型、长度、日期格式和枚举，不接受无法解析的字段直接入库。
9. 展示逐字段确认页；用户可修改、删除或标记“不确定”。
10. 用户确认后写入药物、实际药盒批次、说明书章节和本地文档索引。

### 3.3 结构化识别字段

药盒/药瓶：

- 商品名、通用名、有效成分、规格、剂型
- 厂家、批准文号、条码或追溯码文本
- 批号、生产日期、有效期、储存条件
- 包装中可见的用法用量和警示

说明书：

- 标题、厂家、产品、规格和版本/修订日期
- 成分、适应症、用法用量
- 不良反应、禁忌、注意事项
- 药物相互作用、特殊人群、储存和有效期
- 每个章节对应的图片页码
- 无法确定或图片缺失的字段

### 3.4 本地保存与查看

每次识别保存：

- 原始图片/PDF
- 发送给模型的压缩副本及其哈希
- 请求时间、模型 `deepseek-flash`、提示词版本
- AI 原始响应
- 解析后的结构化 JSON
- 用户修订后的最终文本
- 确认状态、关联药物和药盒批次
- 失败原因和可重试状态

资料页必须支持：

- 按药物、章节、日期和关键词搜索
- 查看原图与结构化内容
- 编辑用户修订文本
- 查看具体页码来源
- 重新识别单页或全部页面
- 删除文件时同步删除数据库索引和缓存
- 无 Key 或离线时先保存原图为“等待识别”，联网后由用户手动继续

### 3.5 视觉识别安全提示词

识别提示词必须规定：

- 只能转录和结构化图片中实际可见内容。
- 不得根据常识补全图片中不存在的剂量、禁忌或日期。
- 看不清时返回 `null` 和不确定原因。
- 不把包装上的广告语言当医学结论。
- 保留页码和原文片段，便于用户核对。
- 输出仅为草稿，不能直接给出“可以一起吃”的结论。

---

## 4. API 与 AI 功能整体修复

### 4.1 统一 DeepSeek 客户端

- Key 保存、文字问答、视觉识别、Harness Web Search 和响应解析使用同一配置与错误模型。
- Base URL 默认固定为官方 `https://api.deepseek.com`；普通用户界面不展示可随意修改的 Base URL。
- 开发者模式才允许使用回环测试网关，禁止任意 HTTP 地址。
- 唯一模型为 `deepseek-flash`。
- 思考深度保留 none/low/high/max，但药盒转录默认使用 low 或 high，不因 max 增加无意义成本。
- 请求支持取消、进度、超时和最多三次有限重试；认证失败不得重试。

### 4.2 连接测试

设置页提供：

- 保存 Key
- 测试连接
- 删除 Key
- 最近一次测试时间和结果

连接测试只发送最小非医疗测试内容，不上传个人档案、药品图片或说明书。

### 4.3 医疗问答

- 先在本地选择与问题有关的用药、过敏、疾病和说明书片段。
- 发送前向用户展示“本次将使用的资料”。
- 需要最新资料时使用 DeepSeek Harness Search/Fetch 流程，不接第三方搜索 API。
- 回答显示来源、证据等级、不确定性、个人资料使用清单和紧急就医提示。
- AI 不得擅自停药、改药或改剂量。

---

## 5. UI/UX 集中改进

本轮不追求花哨视觉，优先修复可用性和信息层级：

- 顶部使用正常 App Bar，显示“个人用药助手”和当前页面名。
- 底部导航在窄屏上不能塞入六个文字按钮；调整为最多五个主要入口，次要页面放入“我的/更多”。
- 所有长表单放入可滚动容器，并保留明确的取消/保存按钮。
- 药物名称、剂量和下一次服用时间成为首页第一视觉层级。
- AI识别显示上传、处理中、解析、待确认、成功和失败状态。
- 错误信息同时给出原因和下一步操作，不显示 Java 异常文本。
- 危险操作二次确认，普通保存操作不应出现不必要的阻塞对话框。
- 适配动态字体、TalkBack标签、48dp触控区域、深色模式和横屏。
- 旧版数据迁移后不能丢失用户已经录入的药物、计划和健康资料。

---

## 6. 实施顺序

### P0：先恢复基本可用

1. 正确实现 Android Keystore。
2. 分离“保存 Key”和“测试连接”。
3. 修复状态栏、导航栏和输入法 Insets。
4. 将产品名统一改为“个人用药助手”。
5. 构建一个只包含上述修复的 APK，完成一次回归。

### P1：替换图片识别路线

1. 删除 ML Kit OCR 依赖和旧 UI。
2. 实现 DeepSeek Flash 图片/PDF视觉请求。
3. 实现严格 JSON Schema、错误处理和字段确认页。
4. 本地保存原图、原始响应、结构化文档和用户修订结果。
5. 将确认结果接入药物、药盒批次和说明书资料库。

### P2：AI与资料联动

1. 修复文字问答和 Harness Search。
2. 完成本地资料选择、发送前确认、引用回链和个人资料使用清单。
3. 旧版 OCR 文档可重新识别。

### P3：界面与稳定性

1. 集中修复导航、表单、加载和错误状态。
2. 检查提醒、药物 CRUD、健康档案、资料删除和备份迁移没有被破坏。
3. 生成最终 APK 和修复报告。

---

## 7. 精简但真实的测试清单

每一项最多两轮修复，不进入长时间审查循环。

### API Key

- 首次保存、重启读取、替换、删除
- 断网保存
- 正确 Key测试连接
- 错误 Key、无余额、限流和超时提示

### 系统栏与界面

- Android 15 edge-to-edge
- 刘海/打孔屏安全区域
- 手势导航和三键导航
- 深色/浅色、键盘弹出和动态字体

### 图片识别

- 一张药盒照片
- 一张有效期/批号照片
- 两到三页说明书
- 模糊照片、旋转照片和无药品内容照片
- JSON解析失败、断网和中途取消
- 用户修改后保存及再次打开

### 回归

- 新增/编辑药物和药盒批次
- 今日任务、已服用、跳过和稍后提醒
- 库存扣减与过期提醒
- 健康档案和本地资料搜索
- AI问答发送前确认和来源显示
- 旧数据库升级且不丢数据

### 构建与安全

- 一次全量单元/仪器测试与 debug APK 构建
- 在可用真机或模拟器上安装启动和关键流程冒烟
- APK 解包扫描，不含真实 Key、用户数据或调试服务器地址

---

## 8. 完成判定

只有满足以下条件才能结束：

- 真机不再出现状态栏重叠。
- 用户可保存、读取、测试和删除 DeepSeek Key。
- App 所有可见位置均不再把 “Luna Max” 作为产品名。
- 本地 ML Kit OCR 已移除。
- 图片/PDF可以通过 `deepseek-flash` 识别，并生成用户可确认、可编辑、可检索的本地资料。
- 药物、提醒、库存、健康档案和 AI 问答基础流程未被破坏。
- 生成新的可安装 APK、修复说明和逐项验收结果。
- 如果没有可用测试 Key，必须完成所有离线与契约验证，并在 App 中提供一步式连接测试入口；不得谎称在线测试通过。

最终输出文件：

- `13_第二轮修复验收报告.md`
- 更新后的用户使用说明和构建说明
- 新 APK 的绝对路径、版本号和 SHA-256

