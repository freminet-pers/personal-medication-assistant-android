# 个人用药助手 v0.4.0：自定义 API 与统一多模态验收报告

日期：2026-09-22（工作区时区）  
版本：v0.4.0  
包名：`com.lunamax.medassistant`  
版本号：`versionCode 4` / `versionName 0.4.0`

## 结论

v0.4.0 的 Provider 配置、三种原生协议适配、统一文字/图片模型、独立搜索、SQLite v6→v7 迁移代码、主题一致性和隐私边界已完成本地验证。真实 DeepSeek API、API 26/API 35、真机和生产签名仍未宣称通过；本轮没有使用真实 API Key。

## 分阶段提交

| 阶段 | 提交 | 内容 |
|---|---|---|
| 1 | `9f28119` | Provider/搜索表、SecretStore 按配置隔离、SQLite v7、旧 Key 迁移 |
| 2 | `a51ce59` | OpenAI Chat/Responses 与 Anthropic Messages 请求适配器、endpoint 和错误映射 |
| 3 | `c45ee31` | 统一多模态仓储、独立搜索仓储、结构化来源和 MockWebServer 合同 |
| 4 | `2cb2440` | AI 服务设置页、助手页、主题 token、明暗主题和无紫色泄漏 |
| 5 | `f4733a7` | Provider workflow 回归测试、按钮/输入框视觉修复、窄屏/夜间/大字号回归 |
| 6 | 本报告提交 | 中英文 README、构建/架构/用户/测试/第三方文档和 v0.4.0 截图 |

## 验收矩阵

| 范围 | 结果 | 证据 |
|---|---|---|
| Android Java/资源编译 | PASS | `:app:testDebugUnitTest :app:assembleDebug`，40 个 Gradle action 成功 |
| Android 单元测试 | PASS | 14/14；DeepSeek、三种 Provider、搜索、错误脱敏、Provider workflow |
| Occurrence 合同 | PASS | `OccurrenceEngineContract PASS` |
| research-gateway | PASS | `npm test`，6/6 |
| APK 元数据 | PASS | package `com.lunamax.medassistant`，versionCode 4，versionName 0.4.0，minSdk 26，targetSdk 35 |
| API 36.1 AVD | PASS | 覆盖安装、冷启动、助手页、AI 服务页、Provider 表单和五页导航无崩溃 |
| 视觉回归 | PASS | 明/暗主题、约 412dp、临时 360dp、系统字号 1.3；截图见 `docs/screenshots/v0.4-*.png` |
| v6→v7 数据边界 | PASS（本地迁移回归） | 覆盖安装到已有本地数据的 AVD，DemoColdCare 仍可显示；未清除用户数据 |
| 真实 DeepSeek 文本/图片/搜索 | 未验证 | 没有真实 `DEEPSEEK_API_KEY`，未执行外部请求 |
| API 26/API 35、真机 | 未验证 | 本机只有 `Medium_Phone_API_36.1` |
| 生产发布签名 | 未验证 | 当前交付为 Debug 签名 APK |

## API 支持矩阵

| Provider | 文字 | 图片 | 协议 | 测试方式 |
|---|---:|---:|---|---|
| 内置 DeepSeek V4.1 Flash | ✓ | ✓ | OpenAI Chat Completions；官方搜索走 Anthropic 兼容入口 | MockWebServer + AVD UI |
| 自定义 OpenAI Chat Completions | ✓ | 需声明并测试 | `POST /chat/completions` | MockWebServer 请求/响应合同 |
| 自定义 OpenAI Responses | ✓ | 需声明并测试 | `POST /responses` | MockWebServer 请求/响应合同 |
| 自定义 Anthropic Messages | ✓ | 需声明并测试 | `POST /messages` | MockWebServer 请求/响应合同 |

内置实际模型 ID 为 `deepseek-flash`；文字和图片共用当前 Provider/模型。新 UI、请求和系统提示中不再使用旧独立 vision 模型名；历史资料记录仍保留读取兼容。

## 搜索与安全边界

- DeepSeek 官方搜索只复用内置 DeepSeek Key。
- 自定义模型必须单独保存 Search Profile、明确选择是否复用 Provider Key、单独测试并返回结构化 URL，助手页才启用联网搜索。
- OpenAI Responses 使用原生 `web_search`；Anthropic/DeepSeek 兼容搜索使用 `web_search_20250305`。
- base URL 仅允许 HTTPS；HTTP 只允许本机回环地址。输入中的已知 endpoint 后缀会被规范化，禁止 userinfo、query 和 fragment。
- Provider Key 与 Search Key 按 ID 分离存储在 Android Keystore；UI、错误状态、备份和导出均不回显或携带完整 Key。
- MockWebServer 响应体中的认证错误不会透传服务端秘密；源码、Git 追踪内容和交付文档的凭据形态扫描为 NONE。

## 交付物

- Debug 构建：`android-app/app/build/outputs/apk/debug/app-debug.apk`
- 根目录副本：`个人用药助手-v0.4.0-debug.apk`
- APK 目录副本：`APK/个人用药助手-v0.4.0-debug.apk`
- 三份文件 SHA-256：`0BDB94DD9BC2133D72C8072D1AFAB4B77A63020D13CE65B35790422F2737F858`
- 匿名截图：`docs/screenshots/v0.4-assistant.png`、`docs/screenshots/v0.4-ai-services.png`
- 用户说明：[docs/用户使用说明.md](docs/用户使用说明.md)
- 构建说明：[docs/构建说明.md](docs/构建说明.md)
- 架构说明：[docs/架构说明.md](docs/架构说明.md)

## 已知限制

真实 API 请求、三种协议对第三方生产网关的具体兼容性、API 26/API 35、实体设备、相机/通知厂商差异和 release 签名均需要后续外部环境补验。AI 输出仍是带来源和不确定性的辅助建议，不替代医生、药师、说明书或急救服务。
