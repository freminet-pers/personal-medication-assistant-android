# 个人用药助手 v0.3.0 产品化重构与完整交付计划

> 本文是交给新建 `gpt-5.6-luna / max` 任务直接执行的决定完备任务书。不得只产出分析、原型或建议；必须完成代码、测试、APK、文档、GitHub 私有交付、独立公开审查和最终公开发布。每个阶段最多两轮集中修复，不做无限审查循环。

## 一、Git 回滚基线与执行分支

新任务开始前，由发起任务完成以下事项：

1. 扫描工作区中的密钥、身份信息、真实健康资料和个人图片。
2. 使用根目录 `.gitignore` 排除本机配置、构建产物、APK、日志、截图缓存、密钥文件，以及 2GB 以上的下载型 `调研证据/` 第三方仓库。
3. 初始化本地 Git，默认分支为 `main`。
4. 创建基线提交 `chore: snapshot v0.2.0 before productization`。
5. 创建注释标签 `v0.2.0-baseline`。
6. 在仓库外创建并验证 `G:\项目\药-backups\personal-medication-assistant-v0.2.0-baseline.bundle`，记录 SHA-256。
7. 创建并切换到 `feature/v0.3.0-productization`。
8. 开发期间每个阶段形成独立提交；禁止使用 `git reset --hard`，需要回退时使用标签、分支或 `git revert`。
9. 本地基线完成后才开始开发；GitHub 远程仓库留到最终验收完成后创建。

## 二、版本与任务完成标准

- 本轮固定为 `versionCode 3`、`versionName 0.3.0`。
- 保持 `applicationId=com.lunamax.medassistant`、现有签名证书和数据库升级兼容性。
- 应用显示名称保持“个人用药助手”，不把开发模型名称显示为产品名。
- 只有同时完成 UI、Key 存储、AI 端到端链路、自动化测试、APK、文档和 GitHub 交付，持续目标才可结束。
- 如果缺少真实设备或安全注入的运行时 Key，必须停在明确的单一外部阻塞点，不能把未执行的验证写成通过。

## 三、P0：修复 Key 存储与 DeepSeek 链路

### 3.1 Key 保存失败

当前实现启用了 `setRandomizedEncryptionRequired(true)`，却在 AES/GCM 加密初始化时手动传入 IV；部分真机会拒绝该组合。必须修复协议本身：

- 加密调用 `Cipher.init(ENCRYPT_MODE, key)`，随后通过 `cipher.getIV()` 保存系统生成的随机 IV。
- 仅解密时使用保存的 IV 和 `GCMParameterSpec`。
- 删除固定零 IV 的兼容性 probe。
- 自检必须真实执行“加密→持久化→重新读取→解密→常量时间比对→删除测试值”。
- 不启用 `setUserAuthenticationRequired(true)`，保存 Key 不依赖锁屏、指纹或系统凭据。
- SharedPreferences 使用能确认结果的 `commit()`，保存后立即读取和解密验证。
- 新增独立 `SecretStore` 和 API Key 专用 Keystore alias；健康资料原有 alias 只用于旧数据兼容。
- 可读取的旧 Key 自动迁移；损坏的旧 Key 只清理 Key 项，绝不能删除药物、资料或健康档案。
- 日志、Toast、错误、测试报告、Git 历史中不得出现明文 Key、密文或认证头。

Key 管理页必须明确区分：未配置、已安全保存但未测试、连接成功、认证失败、余额/权限问题、网络/服务故障。

### 3.2 DeepSeek 接口

- 文字问答和搜索默认使用 `deepseek-v4-flash`。
- 图片识别使用 Flash 家族的 `deepseek-v4-flash-vision-exp`。
- 不使用任何 Pro 模型。
- 思考深度显示“快速、深入、最大”，映射官方 `off / high / max`。
- 执行时锁定 DeepSeek Harness 官方源码 commit，把协议映射到 Android，不嵌入完整 Node.js Harness。
- 搜索采用官方 Harness DeepSeek Provider：官方 Anthropic 兼容端点、`web_search_20250305`、正确的 `x-api-key`/Bearer/`anthropic-version` 头、结构化来源解析、搜索次数和超时限制。
- 参考官方模型适配说明：`https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/llm/llm-deepseek/README.md`。
- 参考官方搜索 Provider：`https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/web/web-search-deepseek/src/provider.ts`。
- 将现有 AI 代码拆成 `SecretStore`、`DeepSeekTransport`、`AssistantRepository`、`VisionRepository`、`HarnessSearchClient`。
- 保存 Key、连接测试、文字问答、图片识别、联网搜索分别呈现独立状态。
- 连接测试不发送个人资料，展示实际模型和结果；区分 401、403、余额不足、429、模型不存在、DNS、TLS、超时和无效响应。
- 增加 MockWebServer 合约测试和日志脱敏测试。
- 真实 Key 只能从应用界面或运行时环境注入，不能写进源码、Gradle、测试资源、命令或 Git。

## 四、UI 与信息架构重构

### 4.1 技术方案

- 保留现有 Java 数据层、提醒和业务规则。
- 从程序化单体 `MainActivity` 重构为单 Activity 外壳、五个 Fragment、XML、ViewBinding、Material Components、RecyclerView 和生命周期感知状态。
- 不进行 Compose 全量迁移。
- `MainActivity` 只负责系统栏、底部导航和页面容器。
- 建立统一 Design System；颜色、字体、间距、圆角、图标、按钮、卡片、表单和动画统一从公共 token/组件取值。

### 4.2 视觉标准

以 Apple 健康和提醒事项的信息组织为参考，同时保留 Android 返回、权限、触控反馈和无障碍习惯：

- 页面背景为浅系统灰，内容表面使用白色或深色分层。
- 主色为系统蓝；完成、提醒、危险分别使用克制的绿、橙、红。
- 使用大标题、分组列表、清晰留白和少量高质量卡片。
- 采用 8dp 间距、16–20dp 连续圆角、至少 48dp 触控区域。
- 使用系统中文字体，建立标题、药名、剂量、时间、正文、辅助信息的清晰层级。
- 不使用表情符号充当图标，不使用紫色 AI 渐变、发光、玻璃效果或同权重卡片堆叠。
- 普通动画 140–280ms，页面过渡不超过 320ms；系统动画关闭时功能完整。
- 浅色和深色主题都必须完成。

### 4.3 五个一级页面

1. **今日**：下一次用药、当日完成进度、按时间排列的任务、已服/跳过/稍后/撤销，以及漏服、库存不足、临期提醒。
2. **药物**：正在服用、家庭药箱、已停用；搜索筛选；详情集中展示剂量、计划、库存、批次、有效期和说明书；支持拍照、资料关联和手工录入。
3. **资料**：分类管理药盒照片、说明书、病历、检查报告；显示缩略图、时间、关联对象和识别状态；固定“本地保存→确认上传→AI 草稿→核对→正式保存”流程。
4. **健康**：基础资料、过敏史、既往疾病、禁忌、检查记录；区分已确认与 AI 草稿；每类资料可以控制是否进入 AI 上下文。
5. **助手**：一级页面；本地会话历史；新建、删除和清空；思考深度、联网搜索、上下文预览；回答分为结论、核对事项、依据、就医提示和不确定性；来源卡展示机构、标题、URL、证据等级和访问时间。

设置从右上角进入，包含 AI 连接、提醒权限、外观、隐私与数据、备份、关于。

### 4.4 完整使用逻辑

- 首次启动提供简短引导和第一种药物创建入口。
- 相机、通知、精确提醒按需申请，不一次索取所有权限。
- 长表单改成分步页面或全屏编辑，不再堆进 AlertDialog。
- 日期、时间、星期、剂量、状态使用选择器，不暴露枚举、位掩码和数据库字段。
- 输入错误显示在字段旁；离开未保存表单时提示继续或放弃。
- 网络请求防重复提交，支持取消、重试和旋转恢复。
- 空、加载、离线、失败、成功状态分别设计。
- 删除操作显示对象和影响；可撤销操作使用 Snackbar。
- 新增本地 AI 会话数据表和显式数据库迁移；API Key 永不进入备份。
- 保持提醒、开机重建、库存、有效期、安全规则的既有业务含义。

## 五、测试与验收

### 5.1 自动化

- 用药计划、跨日、时区、夏令时。
- 库存、临期、过期、丢弃。
- AI 响应、搜索来源、错误映射、日志脱敏。
- Keystore 保存、重启读取、覆盖、删除、故障恢复。
- v0.2.0 数据库升级到 v0.3.0。
- 首次引导、添加药物、创建计划、完成服药、资料识别、健康档案、AI 问答。
- 返回导航、旋转、大字体、键盘遮挡。
- API 正常、401、403、429、5xx、超时、DNS、TLS、模型错误、无效 JSON。

### 5.2 设备和视觉

- 至少使用 API 26 与 API 35 模拟器。
- 检查 360dp、412dp、浅色、深色、默认字号、大字号、手势导航、三键导航、状态栏和 Android 15 Insets。
- 使用匿名演示数据生成五个页面的最终截图。
- 检查滚动、冷启动、页面切换；关闭动画后重复核心流程。

### 5.3 真实 AI

依次验证：

1. Key 保存后退出并重启仍可读取。
2. 最小连接测试返回真实 `deepseek-v4-flash` 响应。
3. 普通文字问答。
4. 经确认后携带最小健康上下文的问答。
5. Harness 搜索返回可点击结构化来源。
6. 匿名药盒/说明书图片经 Flash Vision 生成草稿并保存。
7. 删除 Key 后在线功能正确降级，本地功能继续工作。

没有安全注入的有效 Key 时可以完成 Mock 测试，但不能把真实 AI 标为通过或进入公开发布阶段。

## 六、版本、APK 和文档

- 构建 `个人用药助手-v0.3.0-debug.apk`。
- 核验实际版本、包名、签名和 SHA-256。
- 尽可能执行 v0.2.0 → v0.3.0 覆盖安装。
- 编写 `17_第四轮产品化重构验收报告.md`，区分自动化、模拟器、真机、真实 API 与未验证项。
- README 包含产品简介、五个界面截图和用途、功能清单、AI/搜索/本地资料关系、Key 与隐私、构建安装、架构测试、限制和医疗免责声明。

## 七、GitHub 发布与独立公开审查

- 最终仓库：`cfc450/personal-medication-assistant-android`。
- 验收完成后整理提交并合并到 `main`，保留 `v0.2.0-baseline` 和 `v0.3.0` 标签。
- 创建私有 GitHub 仓库并推送源码、文档、匿名截图、测试说明、隐私说明和 MIT License。
- 创建 `v0.3.0` GitHub Release，附带版本化 APK、SHA-256 和变更说明；APK 不作为普通源码文件提交。
- 私有仓库完成后，主任务必须指派独立的 `gpt-5.6-luna / max` 子任务做公开前审查。
- 审查完整 Git 历史、Key/Token/认证头、个人资料、APK 字符串、README、报告、截图、Gradle、Actions、Release、第三方素材和许可证。
- 审查通过后才改为公开；发现问题则保持私有，修复并清理历史后进行一次集中复审。
- 最终返回 GitHub、Release、APK SHA-256、验收报告、基线标签和 Bundle 位置后，持续目标才可完成。

## 八、默认决定

- v0.3.0 / versionCode 3。
- 保持 applicationId 与签名。
- UI 参考 Apple 健康 + 提醒事项。
- 只使用 DeepSeek Flash 家族，不使用 Pro。
- MIT License。
- GitHub 先私有，独立 Luna Max 审查通过后公开。
- 开发前必须完成本地 Git 基线、标签和仓库外 Bundle。
