# 第三轮界面动画与交互验收报告

验收日期：2026-09-21  
范围：`14_第三轮界面动画与交互优化计划.md` 要求的 Android 当前工程、界面、动效、应用内交互和 v0.2.0 debug APK。

## 结论

第三轮已完成一轮集中实现和一轮构建回归。Android 工程实际改动已落地，未迁移 Compose，未改 applicationId、包名、数据库模型或 DeepSeek 模型策略。

已生成并核验：

- [Gradle 默认 APK](<G:\项目\药\android-app\app\build\outputs\apk\debug\app-debug.apk>)
- [个人用药助手-v0.2.0-debug.apk](<G:\项目\药\个人用药助手-v0.2.0-debug.apk>)

当前构建结果为可交付 debug 包；覆盖安装和真机操作仍需接入 Android 设备完成。

## 已完成的界面与交互

### 全局壳层

- 延续暖白、低饱和玉绿色、琥珀警示和红色危险操作的视觉令牌。
- 顶部改为紧凑品牌栏，页面标题承担当前上下文；子页面提供明确返回按钮。
- 底部固定为五项图标导航：今日、药物、资料、健康、更多；选中态有浅玉绿色指示背景。
- 保留 Android 15 状态栏、导航栏、刘海和键盘 Insets 处理；补充页面、滚动位置和构建页的状态恢复。
- 保留原有数据库、调度器、Keystore 与备份路径，没有进行架构重写。

### 今日

- 主卡片显示今日处理进度、待处理/漏服状态和真实进度条。
- 按夜间、上午、下午、晚上组织时间线；已完成记录降低视觉权重。
- 已服用、跳过、稍后操作保留原有调度含义；服用后显示可撤销反馈。
- 无计划时提供直接进入药物页的下一步操作。
- 安全规则结果改为可读的中文优先级和规则名称。

### 药物、批次与计划

- 增加药名/通用名/规格搜索和全部、正在服用、已停用、有库存、近期到期筛选。
- 药物卡片优先展示名称、规格、状态、库存和到期日期；停用/恢复、删除移入“更多”。
- 批次状态改为“有库存、已开封、已过期、已丢弃”选择器；删除有对象名和后果确认。
- 计划类型改为“每天、每周、按间隔、按需、临时疗程”选择器；星期使用周一至周日选择控件，不暴露位掩码。
- 药物、批次、计划表单使用滚动容器、行内错误和成功反馈；不再要求用户填写内部英文枚举。

### 资料与 AI 识别

- 资料卡片使用“等待识别、正在识别、待确认、已确认、识别失败”等中文状态。
- 真实识别时展示进行中状态；失败状态保留失败原因和重新识别入口，不显示伪造百分比。
- 空资料状态提供导入入口；删除资料前确认，同时清理应用私有目录中的关联文件。
- AI 识别结果仍然先进入草稿确认表单，用户修订后才会写入药物/批次；重新识别不会自动把结果当作已确认数据。

### 健康、更多、设置与助手

- 健康记录使用中文类别选择器；空的详情和来源字段不再显示“无”。
- 健康记录删除有对象确认；空状态仍提供新增记录入口。
- 助手请求增加防重复提交、请求中状态、失败可重试反馈；Key 保存、测试、删除保持分离。
- 助手、设置、关于页的返回统一回到“更多”，更多页返回今日。
- 关于页动态读取 `BuildConfig.VERSION_NAME` / `BuildConfig.VERSION_CODE`，显示版本、构建号和包名，并保留免责声明。

## 动效令牌

| 场景 | 实现 |
|---|---|
| 按压反馈 | 140ms，轻微缩放至 0.985 |
| 页面/卡片入场 | 220ms，透明度 + 8dp 垂直位移 |
| 列表错峰 | 28ms/项，最大延迟 140ms |
| 系统动画关闭 | 读取 `ValueAnimator` 和 `ANIMATOR_DURATION_SCALE`；为 0 时跳过装饰性动画 |
| 状态反馈 | 反馈条约 3.2 秒自动收起；服用记录支持撤销 |

未加入超过 400ms 的阻塞式动画，也未用动画作为唯一状态信息。

## 版本、包名、签名与 APK 核验

- Gradle：`versionCode 2`、`versionName '0.2.0'`
- 实际 APK：`applicationId='com.lunamax.medassistant'`
- 实际 APK label：`个人用药助手`
- APK 签名：`apksigner verify --verbose` 通过，v2 scheme 为 `true`，1 个 signer
- Signer DN：`C=US, O=Android, CN=Android Debug`
- Signer 证书 SHA-256：`d0ea66be2385ade3e3e4e9c3aad0cea3223e352408b8a5250fc5d146dc6cd388`
- Public key SHA-256：`689943c9b69f63d422528fcf5ea9b4f5ebba923857b66bc8c1c6a77403c4f90f`
- 版本化 APK SHA-256：`67B2B7A5E2D282AC926447BDA820E63D1BF31A49D63457737CE61122A1EC9298`
- 默认 APK 与版本化 APK SHA-256 相同：`67B2B7A5E2D282AC926447BDA820E63D1BF31A49D63457737CE61122A1EC9298`

当前工程继续使用本机原有 `C:\Users\32726\.android\debug.keystore`，没有更换签名配置。由于旧版 v1 APK 的 Gradle 默认产物路径被本轮构建覆盖，报告不能声称已完成旧 APK 与新 APK 的离线证书二进制对比；新包证书与现有 debug keystore 指纹一致。

## 构建与测试命令

```powershell
$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
.\gradlew.bat clean :app:assembleDebug :app:testDebugUnitTest --console=plain
```

结果：`BUILD SUCCESSFUL`；clean 构建、Java 编译、资源处理、debug APK 打包和 `testDebugUnitTest` 均通过。

APK 实际信息使用以下工具核验：

```powershell
& "$env:ANDROID_SDK_ROOT\build-tools\35.0.0\aapt2.exe" dump badging <apk>
& "$env:ANDROID_SDK_ROOT\build-tools\35.0.0\apksigner.bat" verify --verbose --print-certs <apk>
Get-FileHash -Algorithm SHA256 <apk>
```

附加扫描结果：版本化 APK 未发现可疑 `sk-`/Bearer 密钥模式；未发现旧产品名 `Luna Max`；APK zip 条目未发现 mlkit、ocr 或 barcode 运行时条目。

`lintDebug` 曾启动但在本机长时间无新增输出后主动停止，本报告不把 lint 结果标记为通过；不影响已通过的 clean build 和 unit test 结论。

## 覆盖安装、数据兼容与未验证项

已完成的兼容性措施：

- applicationId、Java 包名、数据库文件名、`LunaDatabase.VERSION=4` 和现有升级逻辑未改动。
- 没有调用清空数据库、删除用户资料或替换签名的代码路径。
- 旧备份前缀兼容逻辑继续保留。

真机覆盖安装未完成：本机 `adb devices -l` 返回空设备列表，执行 `adb install -r 个人用药助手-v0.2.0-debug.apk` 返回 `no devices/emulators found`。因此以下事项待真机验证：

1. v0.1.0 → v0.2.0 覆盖安装是否被系统接受。
2. 覆盖安装后药物、批次、计划、历史、健康记录和资料索引是否继续可读。
3. 360dp/412dp、深色模式、大字体、三键/手势导航、键盘弹出、旋转重建和 Android 15 真机视觉细节。
4. 相机、图片/PDF 选择、真实 DeepSeek Key、在线成功/超时/失败、AI 草稿确认和重试链路。
5. 通知权限、精确闹钟权限、开机重建和厂商后台限制。

建议真机步骤：先安装旧 v0.1.0 并创建一条药物、一个批次和一个提醒计划；再执行 `adb install -r` 安装本报告 APK；启动后检查今日、药物和健康页，再做一次备份导出/导入和重启恢复。

