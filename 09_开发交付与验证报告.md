# 第二轮开发交付与验证记录

日期：2026-09-19

本轮按 [12_第二轮问题修复与优化计划.md](12_第二轮问题修复与优化计划.md) 完成 P0/P1 主线修复，并将旧版文档中的产品名、旧识别链路和验证结论更新为当前事实。

## 当前交付

- Android 工程：android-app/
- 可安装 debug APK：android-app/app/build/outputs/apk/debug/app-debug.apk
- 研究网关：services/research-gateway/
- 设计令牌：design/tokens.json
- 用户、构建、架构、测试和验收文档：docs/、10_完整功能验收清单.md、13_第二轮修复验收报告.md

## 第二轮实现范围

- Android Keystore 使用 KeyGenParameterSpec 生成 AES/GCM 密钥；保存 DeepSeek Key 后做加密、解密、常量时间比较的本地自测；旧损坏 Key 会清除并提示重新输入。
- 连接测试与 Key 保存分离；401、402、403、429、超时、DNS、TLS 和服务端错误分别给出可理解的提示，连接结果保存在本机。
- target/compile SDK 为 35；根布局通过 WindowInsets 处理状态栏、刘海、导航栏和 IME，Activity 使用 adjustResize，没有依赖固定顶部留白。
- 用户可见产品名统一为“个人用药助手”；deepseek-flash 只作为模型名；通知、标题、备份默认文件名和系统提示均已中性化。
- 移除 ML Kit 文字/条码依赖和 OcrService。图片与 PDF 进入 DeepSeek deepseek-flash 视觉草稿流程：原文件和压缩副本留在应用私有目录，保存页码、SHA-256、原始响应、结构化 JSON、提示版本、状态、失败原因和用户修订；必须确认后才写入药物/批次/资料。
- 相机使用 FileProvider 输出原图，PDF 按页渲染为上传用 JPEG；无 Key 或离线时保留 WAITING_KEY/失败状态，可从资料页重试。

## 验证结果

- :app:compileDebugJavaWithJavac：通过。
- 完整 clean :app:assembleDebug :app:testDebugUnitTest、网关 npm test、APK SHA-256 和最终扫描结果记录在 13_第二轮修复验收报告.md。
- 设备级 Keystore、通知、相机和视觉真实请求是否通过，取决于可用 Android 设备、运行时 Key、网络和 API 额度；没有用假数据冒充在线通过。
