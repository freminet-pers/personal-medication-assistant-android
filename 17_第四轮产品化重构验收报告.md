# 第四轮产品化重构与完整交付验收报告

日期：2026-09-21（工作区时区）
版本：v0.3.0
包名：`com.lunamax.medassistant`
版本号：`versionCode 3` / `versionName 0.3.0`

## 结论

本地代码、构建、协议契约、UI 导航和 Keystore 临时字符串回归已通过。真实 DeepSeek API 与 API 26/35/真机验收未伪造，归并为唯一外部验收项：需要用户提供安全运行时 Key 和目标设备/AVD 后补跑。

## 分阶段提交

| 阶段 | 提交 | 内容 |
|---|---|---|
| 基线 | `7b3e3a7` / `v0.2.0-baseline` | 保留 applicationId、签名配置和可回溯基线 |
| 1 | `76243a4` | Keystore IV 修复、SecretStore、DeepSeekTransport、HarnessSearchClient、文本/视觉仓储、SQLite v5、网关 Messages 协议 |
| 2 | `c2dd5dc` | 单 Activity + 五 Fragment XML/ViewBinding/Material/RecyclerView 产品壳与完整页面交互 |
| 3 | `c9f9a0c` | MockWebServer DeepSeek/Harness 协议契约和稳定错误码测试 |
| 4 | 本报告提交 | README、截图、构建/架构/用户/测试/第三方文档 |

## 验收矩阵

| 项目 | 结果 | 证据 |
|---|---|---|
| Android Java 编译 | PASS | `:app:compileDebugJavaWithJavac` |
| Android 单元测试 | PASS | `:app:testDebugUnitTest`；MockWebServer 3 项协议测试 + occurrence 合同 |
| Debug APK | PASS | `:app:assembleDebug`；v0.3.0 / versionCode 3 |
| research gateway | PASS | `services/research-gateway/npm test`，4/4 |
| occurrence 合同 | PASS | `OccurrenceEngineContract PASS` |
| API 36.1 AVD | PASS | 安装、启动、今日/药物/资料/健康/助手导航，无崩溃；五张截图 |
| SecretStore | PASS（设备级临时回归） | 仅在 AVD 输入临时非真实字符串，保存→重读→删除；最终 Key 状态“未配置” |
| 真实 DeepSeek 文本/搜索/视觉 | 外部阻塞 | `DEEPSEEK_API_KEY` 未设置；未执行请求，不生成伪造回答 |
| API 26/API 35 或真机 | 外部阻塞 | 本机只有 `Medium_Phone_API_36.1`，无物理设备 |

## 安全与隐私检查

- `DataCipher` 与 `SecretStore` 使用不同 Keystore alias；随机 IV 由 Keystore 生成，未保留固定零 IV probe。
- SecretStore 自测使用 `commit()`，验证持久化后再迁移旧偏好；损坏旧 Key 不清理业务数据。
- API Key 不进入 backup bundle、assistant session export、日志或 screenshots；网关 Key 只读运行时环境变量。
- 文本固定 `deepseek-v4-flash`，视觉固定 `deepseek-v4-flash-vision-exp`；搜索使用 Anthropic Messages `/messages` + `web_search_20250305` + 结构化来源。
- 测试夹具不含真实 Key；协议测试使用每次运行生成的 mock header。`git grep` 脱敏扫描未发现可用 `sk-...` 或 Bearer 凭据。

## 交付物

- Debug APK：`android-app/app/build/outputs/apk/debug/app-debug.apk`
- SHA-256：`F6DCC3B0394B6430996FA866A46E2615CA15599FC706457DA115851E2819AE16`
- 截图：`docs/screenshots/v0.3-today.png`、`v0.3-medications.png`、`v0.3-documents.png`、`v0.3-health.png`、`v0.3-assistant.png`
- 用户说明：[docs/用户使用说明.md](docs/用户使用说明.md)
- 架构说明：[docs/架构说明.md](docs/架构说明.md)
- 构建说明：[docs/构建说明.md](docs/构建说明.md)
- 外部条件：[docs/测试与已知限制.md](docs/测试与已知限制.md)

GitHub 私有仓库、独立隐私审计和公开 release 仅在本地验收门禁通过后执行。
