# 调研总结与开发路线建议

核验日期：2026-09-19。本文将 01–06 报告收敛成可执行路线，供同一会话直接写入 `08_详细开发执行计划.md`。

## 1. 是否存在可直接二次开发的完整项目

未发现同时满足中国大陆药品身份/说明书、中文 OCR、本地资料检索、确定性相互作用规则、个人健康档案、DeepSeek Harness 联网、隐私和许可证要求的单一开源项目。

- medTimer 在提醒、库存、过期和离线记录上最成熟，但没有目标中的中国药品资料、OCR、联合检索和医疗安全规则。
- Anshin 的功能面最接近且中文能力强，但本次快照没有根目录 LICENSE，实际构建还受 KSP 依赖阻断，不能直接合入。
- Featherline 的本地加密、备份和提醒值得学习，但 GPL-3.0 及 HRT 专属药代模型不适合作为核心。
- Dose/MediTrak 更像教学或传统实现，不覆盖全部需求。
- DeepSeek Harness 只解决检索/抓取和 Agent 集成问题，不提供 Android UI、药品数据或医疗安全控制；且是 developer preview。

结论：自主开发正式 App，组合“Android 官方能力 + 自有数据模型/规则 + Harness 官方 Web 组件的受控后端”。只借鉴候选架构，不复制候选源码或数据。

## 2. 推荐技术栈与边界

### Android

Kotlin、Compose Material 3、Room/SQLite、DataStore、AlarmManager、WorkManager、Android Keystore、ML Kit Chinese/Barcode。离线基础不依赖网络；精确提醒走 AlarmManager，重启/时区恢复由 SSOT 重建，过期/库存补偿由 WorkManager。

### 本地领域

药物身份、计划、剂量 occurrence、服用事实、库存、有效期、个人健康档案、说明书页/章节、来源、规则和 AI 审计清单全部结构化。状态机先于 UI，时间和时区可注入测试。

### AI/联网

正式 AI 只允许 `deepseek-flash`。健康问答/结构化回答走 Responses API；联网只走 DeepSeek Harness 官方原生 `web_search_20250305` 和 `web-fetch-http`，放受控后端，不接 Bing/Google/Serper 等第三方搜索 API。Harness default 的旧模型字符串必须由适配层覆盖并用测试封锁。

### 医疗安全

重复成分、过敏、疾病禁忌、说明书警告和相互作用规则由结构化规则和 A/B 级来源决定；AI 只解释。所有答案显示证据等级、来源、版本/地区、未知或冲突项，并提供本次使用的个人资料清单。

## 3. 开发顺序

1. 阶段 A：离线手动药物、计划、提醒、Taken/Skip/Snooze/Missed、历史、库存/有效期、基础健康档案。
2. 阶段 B：拍照、条码、ML Kit OCR、字段确认、多页说明书、Room FTS 本地检索和引用。
3. 阶段 C：成分标准化、重复/过敏/疾病冲突、结构化规则和风险来源。
4. 阶段 D：版本化医疗安全提示词、最小资料组装、`deepseek-flash` Responses API、Harness 后端 Search/Fetch、流式/结构化回答、成本与降级。
5. 阶段 E：一次全量构建/测试、可控时钟提醒冒烟、少量 OCR 代表样例、安全/许可证/密钥扫描、Figma 与实现的集中式视觉检查、APK 和文档交付。

## 4. 明确舍弃项

- 不复制 Anshin/medTimer/Featherline/MediTrak 的完整业务代码；许可证、版本耦合和医疗语义不匹配。
- 不在正式 App 内运行完整 Harness Agent，不启用 shell/插件/任意文件/命令能力。
- 不把未授权或覆盖不明的 DrugBank/DDInter/网络爬取数据打包成完整 DDI 库。
- 不默认上传完整病历、完整说明书或原始药盒照片。
- 不实现模型自动 fallback；`deepseek-flash` 不可用时保留本地功能并明确提示。

## 5. 关键待决事项与默认决策

| 事项 | 默认决策 | 影响 |
|---|---|---|
| API Key | 无 Key 也可安装/使用离线功能；测试以临时环境变量注入 | 真实在线调用待用户安全提供 Key |
| 联网部署 | 受控后端 adapter，最小启用 Harness Web 包 | 需要后续部署环境/域名，暂不阻塞 Android |
| 相互作用数据 | 先规则 schema + 官方标签/用户说明书；商业完整库留空 | 早期显示覆盖范围和未知 |
| OCR | ML Kit 本地首选，PaddleOCR 后续候选 | 真实准确率需阶段 B 样本验证 |
| 精确提醒 | `SCHEDULE_EXACT_ALARM` 检查 + 明确授权引导 | 厂商后台限制/用户拒绝需诊断入口 |
| 备份 | 默认排除敏感数据；主动导出应用层加密 | 用户需理解恢复密钥责任 |

## 6. 阶段 1 自检清单

- [x] GitHub 候选池、统一评分、代码级检查、许可证结论。
- [x] 高价值候选完成一次真实构建/最小验证，失败原因未隐藏。
- [x] 中国大陆官方入口、国际补充、相互作用许可缺口。
- [x] OCR/说明书本地库/联合检索比较，未伪造准确率。
- [x] 官方 `deepseek-flash`、URL、effort、Responses/Anthropic、Harness 原生 Web Search/Web Fetch 核验。
- [x] 无 API Key 时的安全处理和离线降级。
- [x] Android 提醒权限、重启、时区、省电与可控时钟测试方案。
- [x] PIPL、Keystore、备份、日志、提示注入、许可证和 developer-preview 风险。
- [x] 7 个强制报告已保存，证据目录保留源码快照与构建记录。

