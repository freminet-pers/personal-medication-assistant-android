# Luna Max 详细开发执行计划

版本：0.1；编写日期：2026-09-19；依据：任务书最新版本、`01`–`07` 调研报告及调研证据。

## 1. 计划目标与非目标

目标是交付一个可安装、能打开、离线记录/提醒主流程可用的 Android 原型，并把 OCR、说明书检索、规则安全检查、`deepseek-flash` 和官方 Harness 联网链路做成可继续扩展的真实边界。不能完成或没有外部条件的功能必须显示“未验证/暂不可用”，不得用静态假结果冒充。

本轮不把完整临床级 DDI 数据库、中国药品实时 API、后台生产部署、正式签名证书、所有厂商省电策略或所有 Android 版本穷举作为阻断条件。

## 2. 阶段交付与优先级

| 优先级 | 交付 | 验收信号 |
|---|---|---|
| P0 | Android 可安装 debug APK；Today → 添加药物 → 计划 → 提醒 → Taken/Skip/Snooze/历史 | 离线完成，数据重启后仍在 |
| P0 | 精确提醒权限状态、通知权限、重启/时区重建骨架 | 无授权时明确告知，不静默丢提醒 |
| P0 | 本地敏感数据最小化、API Key 不落盘、密钥/日志/包扫描 | 扫描无高危残留 |
| P1 | 家庭药箱、库存、有效期和基础健康档案 | 本地 CRUD 与提示可用 |
| P1 | Figma key screens/design system 与代码视觉一致性检查 | 一轮集中调整完成 |
| P1 | 版本化医疗安全 prompt、规则 schema、来源记录与资料清单 | 代码/资源可审计 |
| P1 | 受控 `research-gateway`：只允许 `deepseek-flash`，官方 Harness Search/Fetch 适配、脱敏/限流/引用 | 无 Key 时返回真实可识别的 pending/disabled，不伪造来源 |
| P2 | OCR/条码和说明书分段检索接口 | 无模型或依赖时保留人工确认路径 |
| P2 | Responses API 在线真调用、流式/结构化输出 | 仅在安全临时 Key 注入时验证；否则记录未验证 |

## 3. 正式目录与模块边界

```text
G:\项目\药\
  android-app/                 # 正式 Android 工程；不放候选源码
  services/research-gateway/   # Node/TypeScript 受控后端，密钥只从进程环境读取
  design/                      # Figma 导出/变量/页面说明，不保存用户健康数据
  docs/                        # 用户使用、构建、验证和安全说明
  调研证据/                    # 研究快照、构建和扫描证据
```

Android 内部边界：

- `domain/`：Medication、DosePlan、DoseOccurrence、HealthProfile、Leaflet、RuleFinding、SourceReference 的纯 Kotlin/Java 模型与状态转换。
- `data/`：SQLite/Room（若依赖可用）或受控 SQLite fallback；DataStore/SharedPreferences 只存偏好；私有文件存图片和 OCR 页面。
- `reminder/`：AlarmManager、Receiver、通知动作、权限诊断；只从 SSOT 读取，不让 UI 直接调系统闹钟。
- `safety/`：重复成分/过敏/禁忌确定性规则、证据等级、冲突模型；禁止调用 LLM 决定风险。
- `leaflet/`：图片/条码/OCR adapter、用户确认、章节切分、FTS 查询和页码引用。
- `assistant/`：上下文最小化、prompt 版本、DeepSeek 客户端接口、来源/个人资料审计；网络关闭时本地降级。
- `ui/`：Today、Medications、Cabinet、Health、Assistant、Settings；可访问性文本和敏感通知设置。

Gateway 边界：

- 只开放 `POST /v1/assistant/answer`、`POST /v1/research/search`、`POST /v1/research/fetch` 的最小请求。
- 服务端集中配置 `DEEPSEEK_MODEL = "deepseek-flash"`，启动时拒绝其他值；不接入 Bing/Google/Serper/Exa/Perplexity。
- Search 复用并锁定官方 `web-search-deepseek` wire contract；Fetch 复用官方 `web-fetch-http` 安全策略或保留原包并注明 MIT/third-party notices。
- 只允许来源记录、摘要片段和结构化结果；不开启 Harness shell、代码执行、任意插件、用户文件挂载或任意外连。

## 4. 版本化医疗安全资料

首版资源：

- `medical-safety-system-v1.md`：角色、不替代诊疗、不能擅自停药/改量、急症升级、不确定性、引用格式、提示注入防护。
- `evidence-policy-v1.json`：A/B/C 证据等级、来源字段、地区/版本/厂家/规格匹配、冲突处理。
- `answer-schema-v1.json`：`confirmedFacts`、`ruleFindings`、`uncertainty`、`recommendedAction`、`sourceReferences`、`personalDataUsed`、`disclaimer`。
- `prompt-regression-fixtures.json`：虚构药物、网页注入文本、冲突标签、无资料问题、急症词；不含真实病历和真实 API Key。

上下文固定顺序：版本化 system → 当前问题/时间 → 最少个人资料 → 本地说明书片段 → 规则结果 → 必要联网资料 → 输出 schema。每次回答保存 prompt 版本、资料 ID、来源、模型固定值和时间，不保存隐藏推理。

## 5. UI/Figma 计划

先建立同一套设计 token：米白背景、深墨文字、青绿色主色、暖橙风险色、红色急症色、圆角/间距/字体层级、触控目标最小尺寸和高对比状态。Figma 关键页面：

1. 今日用药：日期、进度、下一次用药、Taken/Skip/Snooze。
2. 添加/编辑药物：药名、规格、剂型、计划、库存、有效期、确认状态。
3. 家庭药箱：库存/过期/临期卡片和来源状态。
4. 安全检查/助手：规则结论、证据等级、来源与本次个人资料。
5. 设置：通知/精确闹钟、隐私、Key、联网、备份、资料版本。

交付顺序：Figma 建立变量/组件与页面 → 代码实现 → 一轮集中视觉检查 → 仅必要修正一轮。若 Figma 连接或创建权限不可用，保存设计 token、页面线框和阻塞证据，不把截图伪装成完成的 Figma 文件。

## 6. 实施步骤

### A. 工程基线

1. 创建独立 `android-app`，设置 `android.overridePathCheck=true` 以处理现有中文父路径，并记录原因；包名、版本和签名均使用占位开发配置，不嵌入任何 Key。
2. 先用最少 Android 依赖确保可安装；只有实际采用且能在构建中解析的依赖才加入。生成 `THIRD_PARTY_NOTICES.md`。
3. 创建 `research-gateway` 的 TypeScript 单元测试和模拟 HTTP provider；不做真实联网假结果。

### B. 离线核心

1. 建表/模型：Medication、Ingredient、DosePlan、DoseOccurrence、DoseEvent、Inventory、HealthProfile、SourceReference。
2. 实现手动添加、编辑、归档、Today 过滤、实际服药状态、库存事务和本地搜索。
3. 实现 AlarmScheduler：权限检查、唯一 request code、精确/降级路径、Receiver 幂等；`Clock` 可注入。
4. 实现重启/时区广播重建和 WorkManager/短时替代路径；不把 WorkManager 当精确提醒。

### C. 说明书/OCR骨架

1. 图片选择/相机输入只写私有目录和哈希；建立 OCR adapter 接口、字段 candidate 状态和人工确认页。
2. 若 ML Kit 依赖可锁定并构建，再启用 Chinese/Barcode；否则保留明确的“需要安装 OCR 模型/手动输入”状态，不伪造识别结果。
3. 说明书分页、章节、FTS/关键词查询、页码引用和版本字段先用本地真实数据流打通。

### D. 安全规则与助手

1. 先实现重复成分、过敏、疾病标签和说明书警告的确定性 finding；每条 finding 都有来源和覆盖范围。
2. 实现最小资料选择器和 `personalDataUsed`；默认不发送完整病历。
3. Android 的 API 客户端只调用 gateway 或用户运行时 Key，不保存共享 Key；日志自动脱敏。
4. Gateway 的正式 model allowlist 只有 `deepseek-flash`；Responses API 走官方 endpoint；Search/Fetch 走官方 Harness 组件；所有外部内容包裹不可信标记。
5. 无 Key/网络/超时/429 时返回真实状态 `AI_UNAVAILABLE`，UI 提供本地规则和来源查看，不显示假答案/假链接。

### E. 文档与交付

写 `docs/用户使用说明.md`、`docs/构建说明.md`、`docs/测试与已知限制.md`，并在 `09_开发交付与验证报告.md` 汇总已验证/未验证/失败原因、APK 路径、扫描、许可证和剩余风险。

## 7. 精简验证矩阵

| 类别 | 一次全量/核心验证 | 证据 |
|---|---|---|
| Android 编译 | 一次 `assembleDebug`，修复后只重跑受影响 task + 一次核心冒烟 | Gradle 输出、APK、SHA-256 |
| JVM/单元测试 | 一次全量；修复后只重跑受影响测试 | JUnit/Gradle 摘要 |
| 提醒 | 注入时钟 + 缩短触发；安装/通知/权限/Receiver/重启模拟 | 测试记录，不真实等待 30 分钟 |
| UI | Figma/代码 key screens 一轮检查，必要时一轮修正 | 页面截图/检查表 |
| OCR | 3 个虚构/公开样例，字段级观察 | 原图哈希、结果、人工确认；不上传真实资料 |
| AI | 无 Key 做离线 contract/mock；有安全 Key 才做一次在线 smoke | 只保留脱敏元数据 |
| 安全/密钥 | 源码、Git 历史、构建缓存、APK/AAB 解包各一次；若高危仅复扫相关部分 | 扫描摘要 |
| 许可证 | 只扫描实际采用依赖 | SPDX/notice 清单 |

## 8. 时间盒与停止规则

- 每个阶段最多两轮检查—修复—复测。
- 同一失败原因连续两次停止；记录日志、根因和下一步，不反复等网络。
- 网络最多三次重试；单次命令无新输出超过合理窗口只诊断一次。
- 不做长时间真实等待或全量重复构建/测试。
- 外部缺少 API Key、签名证书、真机或付费数据源只标记未验证，不阻塞离线交付。
- 低优先级像素、全版本兼容、完整数据覆盖和零警告进入已知限制。

## 9. 阶段转换自检

- [x] 7 份调研报告已读/已自检，任务书 3.6.1、3.6.2、5.4、9.7.1 与第 10/12/13/14 节已纳入。
- [x] 只使用官方 DeepSeek Harness Search/Fetch 方向；没有第三方搜索 API 计划。
- [x] 正式模型固定为 `deepseek-flash`；Harness 旧默认值列为适配风险。
- [x] API Key、个人资料、提示注入、证据分级、规则优先、降级和扫描都有执行项。
- [x] 构建/测试/网络/UI/扫描时间盒符合最新任务书。

计划通过一次一致性检查后直接进入正式开发，不等待外部评审。

