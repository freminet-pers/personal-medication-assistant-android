# 安全、隐私与合规风险调研

核验日期：2026-09-19。产品是个人用药记录/提醒/资料解释工具，不是诊断或处方系统。

## 1. 威胁模型

| 威胁 | 影响 | 防线 |
|---|---|---|
| API Key 进入 APK/Git/日志 | 共享密钥被盗用、费用和数据外泄 | 运行时注入/Keystore/后端代理；禁止常量、`.env` 提交、HTTP body/错误日志记录；发布前扫描源码、Git、缓存、包 |
| 完整病历上传 | 敏感健康信息外泄 | 最小字段选择、默认本地、脱敏、用户确认、可查看“本次使用资料” |
| 网页/OCR 提示注入 | 资料覆盖安全规则、诱导索钥或工具滥用 | 固定版本系统提示词；外部内容不可信；工具白名单；模型无改变证据等级权限 |
| 恶意 URL/SSRF/重定向 | 访问内网、窃取凭据、下载恶意内容 | Harness Web Fetch 的公共地址解析、连接固定、同源重定向、大小/类型/超时限制；正式后端再加域名/地区 allowlist |
| 错误相互作用结论 | 人身伤害 | 结构化规则先行；版本/地区/规格匹配；未知/冲突不降级；医生/药师升级；不指导停药改量 |
| 备份/截图/锁屏泄漏 | 家庭成员或旁观者获得健康信息 | 排除备份或加密导出；Keystore；默认 `FLAG_SECURE`/敏感页面保护；通知公开级别最小化 |
| 第三方依赖/许可证 | 供应链与发布义务 | 版本锁定、依赖清单、许可证扫描、仅复用明确兼容许可；Harness MIT + notices，GPL 候选不合入 |

## 2. 法规和平台事实

- [中国个人信息保护法](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm) 将医疗健康信息列为敏感个人信息，需要特定目的、必要性、严格保护并满足告知/单独同意等要求。个人使用也应按最小化设计，未来公开发布前需做正式合规评估。
- [Android Keystore](https://developer.android.com/privacy-and-security/keystore) 可生成不可导出密钥并限制用途；它不会自动加密数据库明文，需配合 AES-GCM/文件或字段加密。
- [Android 备份最佳实践](https://developer.android.com/privacy-and-security/risks/backup-best-practices) 说明系统备份存在不同设备/版本行为；敏感数据要用 `dataExtractionRules` 排除或做应用层加密导出。
- [Auto Backup](https://developer.android.com/identity/data/autobackup) 与 [FLAG_SECURE](https://developer.android.com/security/fraud-prevention/activities) 只能降低常见泄漏，不能承诺对所有厂商/录屏/恶意系统有效。

## 3. 数据流设计

```text
相机/手动录入
  -> 本地 OCR/用户确认 -> 加密本地 Room/文件/FTS
  -> 确定性重复成分/过敏/说明书规则
  -> 仅在用户触发 AI 时选取最小脱敏上下文
  -> 受控后端：deepseek-flash + 官方 Harness Search/Fetch
  -> 返回答案、来源、规则结果与本次资料清单
  -> Android 本地保存会话摘要（不保存隐藏推理）
```

正式后端只允许向 DeepSeek 发送：当前问题、必要的结构化字段、命中的说明书片段、规则结果和必要的公开来源。默认不发送姓名、身份证、住址、完整检查报告、完整病历或未命中的说明书页面。

## 4. 密钥与日志

开发测试：只接受进程级临时 `DEEPSEEK_API_KEY` 或运行时人工输入；不把真实 Key 放入任务书、聊天、源代码、Git 历史、Gradle、APK/AAB、截图、报告、终端输出和缓存。日志只输出 request id、模型固定字符串、状态码、耗时和 token 元数据，不输出 Authorization、请求全文或完整病历。测试数据只能是虚构人物/药物。

正式方案比较：

- 用户自带 Key：个人原型部署简单，Key 用 Keystore 加密，用户能删除；客户端直连仍可能被设备/调试环境取用。
- 自有后端代理：客户端没有共享 Key，便于限流、审计和来源过滤，但需要部署、费用与服务器隐私承诺。

本项目先实现“运行时 Key + Keystore/受控后端接口可替换”的边界；没有 Key 时保留离线能力并记录待验证，不阻塞安装包。

## 5. Harness 与第三方风险

DeepSeek Harness 许可证为 MIT，但官方安全声明是开发者预览、未完成安全审计，能够访问网络/进程/凭据/文件并执行模型代码。正式系统不直接在 Android 运行完整 Agent，不开放 shell/文件/插件能力；只在隔离后端启用官方 `web-search-deepseek` 与 `web-fetch-http`，容器最小权限、无本地用户文件挂载、无长期 Key 写盘、网络 egress 白名单。

ML Kit、PaddleOCR、AndroidX、SQLCipher 等依赖要按实际版本生成 notices。候选项目的 GPL-2.0/GPL-3.0 代码与未确认许可证项目不进入正式产品。

## 6. 用户数据权利与产品提示

提供查看、修改、导出、删除药物/健康档案/说明书/会话、撤销联网/Key、清除缓存和关闭备份的入口。每次联网前提示发送哪些字段；回答顶部固定“资料解释，不替代医生”，急症/严重过敏/呼吸困难/意识异常等触发紧急就医提示。医疗安全提示词和规则文件版本化，变更需回归测试并展示版本号。

