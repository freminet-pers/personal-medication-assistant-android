# 个人用药助手：第二轮修复验收报告

日期：2026-09-19
依据：12_第二轮问题修复与优化计划.md

## 总结

P0 和 P1 主线已经落到 Android 工程并完成构建级验证：Keystore 加密自测、独立连接测试、Android 15 Insets、产品命名收口、移除 ML Kit/OcrService、DeepSeek deepseek-flash 视觉草稿、原图/压缩副本/响应/结构化结果/用户修订留存，以及确认后入库均有代码路径。

当前没有可用 adb 设备和真实 DeepSeek Key，因此真机 Keystore、通知/相机/PDF/系统栏截图和真实在线视觉请求不能标记为通过；报告按外部条件如实保留。

## 逐项验收

| 优先级 | 项目 | 实现证据 | 结果 |
|---|---|---|---|
| P0 | AES/GCM Keystore | DataCipher 使用 KeyGenParameterSpec；用途 encrypt/decrypt、GCM、NoPadding；保存 Key 后执行加密、解密和 MessageDigest 常量时间比较；损坏运行时 Key 在读取时清除。 | 代码已实现；真机行为待设备 |
| P0 | 保存 Key 与测试连接分离 | 设置/助手分别提供保存和“测试 DeepSeek 连接”；保存不联网，成功提示“已安全保存；尚未测试连接”。 | 代码已实现 |
| P0 | 连接错误可理解 | AiClient 映射 401、402、403、429、5xx、超时、DNS、TLS 及一般 HTTP 错误；测试结果本机留存。 | 代码已实现 |
| P0 | Android 15 系统栏 | targetSdk/compileSdk 35；setDecorFitsSystemWindows(false)；根容器消费 statusBars、displayCutout、navigationBars、IME Insets；底部导航消费底部 inset；adjustResize。 | 代码已实现；截图待设备 |
| P0 | 产品命名 | App label、首页、通知、公开锁屏通知、备份默认文件名和安全 system prompt 使用“个人用药助手”；deepseek-flash 只作为模型技术信息。 | 源码/资源/APK 资源扫描通过 |
| P1 | 删除旧识别路线 | build.gradle 无 ML Kit 依赖；OcrService.java 不存在；UI 文案为 AI 识别草稿；旧资料可标记为旧版并重试。 | 源码扫描通过 |
| P1 | 图片视觉识别 | 图片方向校正、压缩；相机使用 FileProvider 原图；DeepSeek deepseek-flash 多模态请求只上传压缩副本，response_format 为 JSON object，提示内含严格 JSON Schema；本地校验字段类型、数组长度和日期。 | 代码已实现；在线调用待 Key/网络 |
| P1 | PDF 视觉识别 | PDF 私有复制后按页 PdfRenderer 渲染为 JPEG；每页单独进入队列并可保留页码。 | 代码已实现；设备/真实文件待验证 |
| P1 | 资料留存与重试 | 数据库保存原/压缩路径和 SHA-256、MIME、页码、模型、提示版本、请求时间、原始响应、结构化 JSON、可见文本、用户修订、状态和失败原因；WAITING_KEY、PROCESSING、DRAFT、CONFIRMED、FAILED 路径存在。 | 代码已实现 |
| P1 | 用户确认后入库 | 识别结果先进入确认表单；用户可修改商品名、通用名、成分、规格、厂家、批准文号、追溯码、批号、日期、说明书字段；确认后才写药物/批次/资料。 | 代码已实现 |
| P2 | 文本助手/联网边界 | 固定 deepseek-flash；发送前显示最小资料并二次确认；官方 Search/Fetch 保留同源、公开地址、内容类型、大小和不可信数据边界。 | 网关测试通过；在线条件待 Key |
| P2 | 计划、提醒、库存和安全规则 | 既有 OccurrenceEngine、ReminderScheduler、事务库存和 SafetyEngine 保留；第二轮未破坏其接口。 | 合同测试和构建通过 |
| P3 | UI 与交付文档 | 五项底部导航，助手/设置进入“更多”；说明、构建、架构、测试、验收文档已更新；设计 token 产品名已中性化。 | 已完成 |

## 可复现测试结果

### Android

命令：

    Set-Location G:\项目\药\android-app
    $env:JAVA_HOME = C:\Program Files\Android\Android Studio\jbr
    .\gradlew.bat clean :app:assembleDebug :app:testDebugUnitTest --no-daemon --console=plain

结果：BUILD SUCCESSFUL；38 actionable tasks；Android Java 编译、资源处理、debug APK、debug unit test 均通过。

### occurrence 合同测试

结果：OccurrenceEngineContract PASS。

覆盖：每日多次、每周、跨午夜 INTERVAL、起止日期、暂停状态。

### research gateway

命令：services/research-gateway 下执行 npm test。

结果：4 tests、4 pass、0 fail。覆盖模型白名单、Key 不进入可序列化记录、私网/凭据 URL 拒绝、医学提示与注入防护。

### APK 与扫描

- APK：android-app/app/build/outputs/apk/debug/app-debug.apk
- 文件大小：2,049,638 bytes
- SHA-256：76085171A1EC75888E64CB5AE205E35B7DE77D231B59B3A2803878D88E052E33
- APK archive 未发现 mlkit、ocr 或 barcode 条目。
- APK 资源未发现 Luna Max/luna max 用户可见字符串。
- 源码未发现 ML Kit/OcrService/条码依赖或可用 sk-.../Bearer 凭据模式。

## 外部条件与未宣称项

本轮执行 adb devices 后没有在线设备；因此没有宣称以下项目已真机通过：Android Keystore 硬件/系统行为、退出重进后 Key 保持、通知动作、相机权限与原图、PDF 实机渲染、Android 15 状态栏/IME 截图、亮暗色和多屏幕布局。

未配置真实 DeepSeek Key、网络和余额；因此没有发出在线医疗请求，也没有把模型返回伪装成测试通过。生产交付前仍需 release 签名、认证、限流、网关出口控制、脱敏日志和医疗/隐私合规审查。
