# 个人用药助手：v0.3.0 构建与安装说明

## Android 工程

环境：Android Studio JDK 17、Android SDK platform 35、Build Tools 35.0.0、Gradle wrapper 8.7、Android Gradle Plugin 8.6.1。`applicationId` 保持 `com.lunamax.medassistant`；本轮为 `versionCode 3` / `versionName 0.3.0`。工程的 `local.properties` 不应提交。

```powershell
Set-Location 'G:\项目\药\android-app'
$env:JAVA_HOME = 'C:\Program Files\Android\Android Studio\jbr'
.\gradlew.bat :app:clean :app:assembleDebug --no-daemon --console=plain
```

单元测试包含 Android mockable jar 在中文路径下的 Windows worker 编码限制。若 `testDebugUnitTest` 报测试类找不到，使用临时 ASCII 映射盘运行（不复制或修改源码）：

```powershell
subst X: 'G:\项目\药'
Set-Location X:\android-app
$env:JAVA_HOME = 'C:\Program Files\Android\Android Studio\jbr'
.\gradlew.bat :app:testDebugUnitTest --no-daemon --console=plain
subst X: /D
```

APK：`android-app/app/build/outputs/apk/debug/app-debug.apk`。安装：

```powershell
$sdk = 'C:\Users\32726\AppData\Local\Android\Sdk'
$adb = Join-Path $sdk 'platform-tools\adb.exe'
& $adb install -r 'G:\项目\药\android-app\app\build\outputs\apk\debug\app-debug.apk'
& $adb shell am start -n com.lunamax.medassistant/.MainActivity
```

## 合同与网关测试

```powershell
# 在 X:\android-app 下运行 Android 单元测试
.\gradlew.bat :app:testDebugUnitTest --no-daemon --console=plain

Set-Location 'G:\项目\药\services\research-gateway'
npm test
```

Occurrence 合同测试也可直接运行：

```powershell
$temp = Join-Path $env:TEMP 'personal-medication-occurrence-contract'
New-Item -ItemType Directory -Force -Path $temp | Out-Null
$javac = 'C:\Program Files\Android\Android Studio\jbr\bin\javac.exe'
$java = 'C:\Program Files\Android\Android Studio\jbr\bin\java.exe'
& $javac -d $temp 'android-app/app/src/main/java/com/lunamax/medassistant/OccurrenceEngine.java' 'android-app/app/src/test/java/com/lunamax/medassistant/OccurrenceEngineContract.java'
& $java -cp $temp com.lunamax.medassistant.OccurrenceEngineContract
```

## 运行时 Key 与网关

Key 只能从应用运行时输入或安全环境变量提供；不得写入脚本、文档、Git、日志、APK 或测试夹具。没有 Key 时不执行在线连接、搜索或视觉请求。

```powershell
Set-Location 'G:\项目\药\services\research-gateway'
$env:DEEPSEEK_API_KEY = '<runtime-only-key>'
node src/server.mjs
Remove-Item Env:DEEPSEEK_API_KEY
```

网关只绑定回环地址；生产部署仍需认证、限流、出口控制、脱敏日志和密钥轮换。
